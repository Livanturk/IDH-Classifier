# AUTO-GENERATED ablation config (base: configs/simsiam_brats.yaml).
# Axes: backbone {resnet18|resnet50} x UPenn {included|excluded} x crop_mode {brain|tumor_margin}.
# crop_mode=tumor_margin uses seg ONLY to locate the crop (never as network input).
# NOTE: if resnet50 OOMs on your GPU, lower batch_size (LR auto-scales with it).

data_root: BraTS2021/BraTS2021_TrainingSet
splits_file: splits/splits.json
modalities:
- t1
- t1ce
- t2
- flair
roi_size:
- 96
- 96
- 96
crop_mode: brain
tumor_margin: 16
num_workers: 8
cache_rate: 0.0
backbone: resnet18
feature_dim: 256
proj_dim: 256
proj_hidden_dim: 512
pred_hidden_dim: 128
optimizer: sgd
base_lr: 0.05
weight_decay: 0.0001
momentum: 0.9
epochs: 200
batch_size: 16
warmup_epochs: 10
fix_pred_lr: true
amp: true
grad_clip: 0.0
out_dir: checkpoints/simsiam_r18_all
seed: 42
save_every: 20
log_every: 10
resume: null
